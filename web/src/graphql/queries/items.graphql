query Items($limit: Int!, $cursor: String) {
    items(limit: $limit, cursor: $cursor) {
        hasMore
        items {
            id
            createdAt
            updatedAt
            title
            description
            textSnippet
        }
    }
}
