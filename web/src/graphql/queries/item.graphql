query Item($limit: Int!, $cursor: String) {
    item(limit: $limit, cursor: $cursor) {
        hasMore
        item {
            id
            createdAt
            updatedAt
            title
            description
            textSnippet
        }
    }
}
