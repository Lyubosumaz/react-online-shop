query Item($id: Int!) {
    item(id: $id) {
        id
        category
        title
        description
        price
        rating
        voteStatus
        creator {
            id
            username
        }
        createdAt
        updatedAt
    }
}
