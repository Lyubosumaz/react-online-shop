query Item($limit: Int!, $cursor: String) {
    items(limit: $limit, cursor: $cursor) {
        hasMore
        item {
            id
            createdAt
            updatedAt
            title
            description
            textSnippet
            creator {
                id
                username
            }
        }
    }
}
