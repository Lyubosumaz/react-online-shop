query Item($id: Int!) {
    item(id: $id) {
        id
        createdAt
        updatedAt
        title
        points
        description
        voteStatus
        creator {
            id
            username
        }
    }
}
