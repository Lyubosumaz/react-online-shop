query Item($id: Int!) {
    item(id: $id) {
        id
        createdAt
        updatedAt
        title
        rating
        description
        voteStatus
        creator {
            id
            username
        }
    }
}
