query Item($id: Int!) {
    item(id: $id) {
        id
        createdAt
        updatedAt
        title
        points
        text
        voteStatus
        creator {
            id
            username
        }
    }
}
