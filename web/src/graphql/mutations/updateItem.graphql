mutation UpdateItem($id: Int!, $category: String!, $title: String!, $description: String!, $price: String!) {
    updateItem(id: $id, category: $category, title: $title, description: $description, price: $price) {
        id
        category
        title
        description
        descriptionSnippet
        price
    }
}
