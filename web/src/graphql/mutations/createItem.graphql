mutation CreateItem($input: ItemInput!) {
    createItem(input: $input) {
        id
        createdAt
        updatedAt
        category
        title
        description
        price
        rating
        creatorId
    }
}
