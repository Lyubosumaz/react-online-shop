mutation CreateItem($input: ItemsInput!) {
    createItem(input: $input) {
        id
        title
        description
        price
        customerId
        creator {
            id
            username
            email
        }
        createdAt
        updatedAt
    }
}
