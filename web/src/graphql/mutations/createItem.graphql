mutation CreateItem($input: ItemsInput!) {
    createItem(input: $input) {
        id
        title
        description
        price
        creatorId
        creator {
            id
            username
            email
        }
        createdAt
        updatedAt
    }
}
