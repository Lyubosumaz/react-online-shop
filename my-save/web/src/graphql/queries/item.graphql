query Item($id: Int!) {
    item(id: $id) {
        id
        title
        description
        # voteStatus
        price
        rating
        creator {
            id
            username
        }
        createdAt
        updatedAt
    }
}
